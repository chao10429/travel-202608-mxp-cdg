# GitHub Pages 上傳檔案

這個資料夾整理了網站上線需要的乾淨檔案：

- `index.html`
- `favicon.png`
- `styles.css`
- `data.js`
- `app.js`

如果 GitHub Pages 來源設定為 repository root，請上傳「此資料夾內的檔案」到 repo 根目錄，不是把 `github-pages` 整個資料夾上傳到根目錄。

如果想改用資料夾部署，可把這個資料夾改名為 `docs`，並在 GitHub Pages 設定中選擇 `main /docs`。
